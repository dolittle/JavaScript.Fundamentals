{
  "name": "@dolittle/booting",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "transpile": "rm -rf dist && PACKAGE_DISTRIBUTION=true npx babel . -d dist --config-file ../../.babelrc",
    "prepublish": "yarn run transpile"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@dolittle/core": "1.0.3",
    "rxjs": "^6.3.3"
  },
  "devDependencies": {
    "@babel/cli": "^7.2.0"
  }
}
